# Terraform configurado como Modulos

Modulos podem chamar outros modulos.
Podemos reutilizar os módulos para vários projetos.

Mais complexo.

Os modulos ficam dentro das pastas dos ambientes.

`$ cd dev`
`$ terraform init`
`$ terraform plan`
`$ terraform apply `